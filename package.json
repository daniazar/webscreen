{
  "name": "web-screener",
  "version": "1.0.0",
  "description": "a website screenshot generator using pupetter and a JSON file. Intended to be used in combination as command line tool to generate images of of components before commit.",
  "main": "index.js",
  "scripts": {
    "start": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc && node ./build/index.js"
  },
  "bin": {
    "websceen": "./build/index.js"
  },
  "author": "Daniel Azar",
  "license": "ISC",
  "dependencies": {
    "filenamify-url": "^1.0.0",
    "puppeteer": "^1.9.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.1.3"
  },
  "devDependencies": {
    "@types/node": "^10.12.0",
    "@types/puppeteer": "^1.9.0",
    "tslint": "^5.11.0"
  }
}
